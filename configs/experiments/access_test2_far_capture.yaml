extends: ../defaults.yaml

lookahead:
  values_m: [0.50]

initial_condition:
  e_y_m: 0.5
  e_psi_deg: 90.0

gain_sweep:
  rho: [3.0]
  zeta: [1.0]
  representative:
    rho: 3.0
    zeta: 1.0

paths:
  - key: straight
    label: Straight
    type: straight
    params:
      length: 6.0
      num_points: 600
    evaluation:
      length: 4.0
      num_points: 400

experiment:
  max_steps: 1100
  output_dir: results/access_test2_far_capture
  export_tex_project_dir: ../tex_docker_environment/projects/fumiya_ieee_access
