extends: access_test2_far_capture.yaml

methods: [PP, DPP, ECPP without gate, ECPP, ECPP ey]

gate:
  mode: sigmoid

experiment:
  output_dir: results/gate_ablation_far_capture
  export_tex_project_dir: ""
